const API_BASE_URL = "http://localhost:5000/api";

/*
    SMARTCLEAN CONFIGURATION

    DEMO_MODE = true
    ----------------
    Use this for your SIH frontend demo.
    Complaints and login work using browser localStorage.

    DEMO_MODE = false
    -----------------
    Use this when your Node.js backend is running
    on http://localhost:5000
*/

const DEMO_MODE = true;